import 'package:flutter/material.dart';

/// Clase que define una paleta de colores para la aplicación.
class AppColors {
  /// Color primario de la aplicación.
  static const primary = Color(0xFF07677F);

  /// Color blanco, utilizado generalmente para texto o fondos claros.
  static const white = Color(0xFFFFFFFF);

  /// Color de fondo principal de la aplicación.
  static const background = Color(0xFFD9D9D9);

  /// Color utilizado para los textos o elementos sobre el fondo principal.
  static const onBackground = Color(0xFF171C1F);

  /// Color de las superficies, como tarjetas o áreas elevadas.
  static const surface = Color(0xFFF7F9FF);

  /// Color utilizado para los textos o elementos sobre las superficies.
  static const onSurface = Color(0xFF191C20);

  /// Color variante para elementos sobre la superficie, como textos secundarios.
  static const onSurfaceVariant = Color(0xFF40484C);
}
